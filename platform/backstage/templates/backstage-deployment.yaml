apiVersion: apps/v1
kind: Deployment
metadata:
  name: backstage
  namespace: backstage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backstage
  template:
    metadata:
      labels:
        app: backstage
    spec:
      containers:
      - name: backstage
        image: ghcr.io/backstage/backstage:latest
        args: ["--config", "app-config.production.yaml"]
        env:
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: backstage-app-secret
              key: POSTGRES_PASSWORD
        # GITHUB_TOKEN is needed for catalog import if repos are private
        # For now we use a placeholder or you can add it to your GCP Secret Manager
        - name: GITHUB_TOKEN
          value: "not-set"
        ports:
        - containerPort: 7007
          name: http
        volumeMounts:
        - name: config
          mountPath: /app/app-config.production.yaml
          subPath: app-config.production.yaml
      volumes:
      - name: config
        configMap:
          name: backstage-config
---
apiVersion: v1
kind: Service
metadata:
  name: backstage
  namespace: backstage
spec:
  ports:
  - port: 7007
    targetPort: http
  selector:
    app: backstage
