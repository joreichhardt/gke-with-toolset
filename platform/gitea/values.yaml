gitea:
  config:
    APP_NAME: "Gitea: Git with a cup of tea"
    actions:
      ENABLED: true
    server:
      SSH_DOMAIN: "gitea.hannesalbeiro.com"
  postgresql:
    enabled: true
  postgresql-ha:
    enabled: false
  redis-cluster:
    enabled: false
  mongodb:
    enabled: false
